import { installInfo } from '@/types'

import v030 from '@utils/version/v0.3.0'

const data: installInfo = {
    version: '0.4.0',
    uiExtension: v030.uiExtension,
    right: v030.right,
}

export default data
